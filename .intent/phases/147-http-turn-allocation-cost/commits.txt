734df13 Measure HTTP turn and allocation cost

Evidence:

- cargo fmt --all --check
- git diff --check
- cargo test -p tina-proof-harness perf::tests::hotpath_report_prints_ns_us_and_named_stages -- --nocapture
- cargo test -p tina-http parse::tests:: -- --nocapture
- cargo test --release --manifest-path examples/systems/perf_native/Cargo.toml --test perf -- --nocapture
- cargo test --release --manifest-path examples/systems/perf_native/Cargo.toml --test hotpath -- --nocapture
- cargo clippy -p tina-http -p tina-proof-harness --all-targets -- -D warnings
- cargo clippy --release --manifest-path examples/systems/perf_native/Cargo.toml --all-targets -- -D warnings
- make perf-record
- make perf-check
- cargo test -p tina-http --tests -- --nocapture
- make proof-fast

Recorded history:

- .intent/phases/147-http-turn-allocation-cost/perf_history.jsonl

c41aafa Coalesce small buffered HTTP responses

Evidence:

- cargo fmt --all --check
- git diff --check
- cargo clippy -p tina-http --all-targets -- -D warnings
- cargo test -p tina-http --test server_smoke -- --nocapture
- cargo test -p tina-http --test body_metrics -- --nocapture
- cargo test -p tina-http --test dst_simulator saved_seed_interleaving_fingerprint_is_stable -- --nocapture
- cargo test --release --manifest-path examples/systems/perf_native/Cargo.toml --test hotpath -- --nocapture
- make perf-record
- make perf-check

Recorded history:

- .intent/phases/147-http-turn-allocation-cost/perf_history.jsonl
