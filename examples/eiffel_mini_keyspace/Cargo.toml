[package]
name = "eiffel-mini-keyspace"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
tina = { path = "../../tina" }
tina-runtime = { path = "../../tina-runtime" }
tokio = { version = "1", features = ["io-util", "net", "rt", "sync"] }

[workspace]
