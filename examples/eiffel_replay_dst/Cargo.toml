[package]
name = "eiffel-replay-dst"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
tina = { path = "../../tina" }
tina-runtime = { path = "../../tina-runtime" }
tina-sim = { path = "../../tina-sim" }
tokio = { version = "1", features = ["macros", "rt", "sync", "time"] }

[workspace]
